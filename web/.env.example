# Deployed DayOne contract on Monad Testnet
VITE_CONTRACT=0x...

# Testnet-only key the dashboard signs with, so payout batches can be fired
# concurrently without a wallet prompt per batch. Never a key holding real assets.
VITE_DEMO_KEY=0x...
